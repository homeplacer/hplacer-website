{
  "name": "hplacer-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Home Placer employee operations portal — a separate Cloudflare Worker from the public hplacer.com site.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.ts",
    "dev": "node dev/server.ts",
    "migrate:local": "node dev/migrate.ts"
  }
}
